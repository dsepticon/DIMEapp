# Deployment is intentionally blocked until the user approves a staging plan.
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "DIME deployment requires explicit approval; see docs/deployment.md"
        - exit 1
  artifacts:
    baseDirectory: dist/frontend
    files:
      - "**/*"
