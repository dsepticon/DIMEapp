--- published
+++ candidate-unpublished
@@ -42,4 +42,5 @@
-        <strong>Effective date: September 15, 2026.</strong> DIME is a mining game for Twitch. DIME is
-        operated by Dsepticon. This policy covers the DIME extension, including the Milestone 4 Destroya
-        Industries original-universe gameplay prepared for Hosted Test. It does not describe Twitch's own
-        privacy practices.
+        <strong>Effective date: September 16, 2026.</strong> DIME is a mining game operated by Dsepticon.
+        This policy covers the DIME Twitch extension, the website Guest Demo, and controlled website
+        authentication, including Milestone 4 Destroya Industries original-universe gameplay. Public website
+        sign-in and account linking are currently disabled. Separately authorized invitation-only tests may
+        be enabled before public sign-in. This policy does not describe Twitch's own privacy practices.
@@ -67,2 +68 @@
-        orders, quests, and progression while replacing content identifiers and names. Permanent conversion
-        begins disabled and is controlled by the server. If conversion is later enabled, an idempotent
+        orders, quests, and progression while replacing content identifiers and names. Conversion is controlled by the server. When a conversion is performed, an idempotent
@@ -88,0 +89,57 @@
+      </p>
+
+      <h2>Website Guest Demo</h2>
+      <p>
+        The Guest Demo at /game/ is playable without an account. Demo gameplay remains only in the current
+        page's memory: it creates no account, identity, authentication session, manifest, server save, action
+        receipt, or pending server action. It does not save gameplay progress in cookies, local storage,
+        session storage, or the game database. Reloading starts a fresh demo and separate tabs are independent.
+        The current guest build does not retain display preferences. AWS still processes connection information
+        needed to deliver and secure the public website.
+      </p>
+
+      <h2>Controlled website sign-in and shared-save linking</h2>
+      <p>
+        Website sign-in, when enabled for authorized testers or the public, uses a separately registered Twitch
+        OAuth/OpenID Connect application. DIME requests only the openid scope for authentication, not email,
+        profile-picture, chat, or channel-management access. The server validates Twitch's authorization and
+        transiently uses the numeric Twitch account ID to derive a protected credential identifier. Website
+        OAuth and the Extension's opaque identity use separate credentials. DIME assigns a random internal
+        account ID and stores encrypted authentication and credential-association records. Provider access
+        and refresh tokens stay encrypted on the server, not in browser storage or completed callback URLs.
+      </p>
+      <p>
+        Invitation-only sign-in requires an owner-issued link intended for one Twitch account. Its signed
+        contents are a version, expiry, random nonce and HMAC-protected tester identifier, not a raw Twitch ID,
+        email address, DIME gameplay identity, or provider token. A link expires within 15 minutes and can
+        start sign-in only once. The server stores a digest of the used nonce and its expiry to reject replay;
+        short-lived encrypted login state carries the intended tester check until callback or expiry. A wrong
+        Twitch account is rejected before creating a DIME identity, account, session, manifest or save, and
+        DIME attempts to revoke rejected provider tokens. Revocation can temporarily fail at Twitch; no local
+        access is granted by that failure. Keep invitation links private and do not email them to support.
+      </p>
+      <p>
+        Website authentication uses Secure, HttpOnly, host-only cookies. Login and session cookies use
+        SameSite=Lax; verified deletion recovery uses SameSite=Strict. The login cookie binds a short-lived
+        authorization request to the browser. The session cookie represents authenticated access; a new sign-in
+        rotates the active session, and logout invalidates it, expires cookies and requests provider-token
+        revocation. Exact-origin and anti-CSRF checks protect changes. An in-memory CSRF value is not a
+        gameplay save. The Guest Demo does not create or use these authentication cookies, and the Extension does not
+        set DIME authentication cookies.
+      </p>
+      <p>
+        Sign-in and linking are independently controlled. During sign-in-only testing, a web session may be
+        created but no gameplay save is read, created or changed while linking is disabled. When separately
+        enabled, linking requires deliberate confirmation, independent authentication on both sides, and a
+        short-lived one-use intent. An existing Extension save remains canonical, including its generation,
+        inventory, wallet, equipment, ships, quests, position, mining state and technical receipts. Two existing
+        saves are not automatically merged or overwritten; conflicts require verified support resolution.
+        Transactions, revisions and idempotent outcomes prevent duplicate linking. Unlinking removes the
+        appropriate identity association without deleting gameplay data or silently creating a replacement save.
+      </p>
+      <p>
+        A bounded deletion manifest records stable references to the account's persistent record classes,
+        including gameplay state, identity mappings, linking, authentication and recovery records. It holds no
+        provider tokens, secret values, gameplay snapshots or personal profile fields. Each account supports
+        at most one Extension identity and one web OAuth identity. Temporary record classes are identified by
+        their bounded partition or class, rather than accumulating every temporary record identifier.
@@ -125,0 +183,24 @@
+      <p>
+        Invitation-use records expire within 15 minutes. OAuth login state expires after at most five minutes
+        or at the earlier invitation expiry. Sessions and encrypted provider grants expire after at most eight
+        hours; sessions stop authorizing requests after 30 minutes idle. Tester eligibility ends with the
+        session's eight-hour limit and each new sign-in needs a new invitation. Link intents expire after five
+        minutes; successful link outcomes expire after 24 hours. Rejected or conflicting links create no new
+        identity mapping or manifest entry; any previously issued intent keeps its five-minute limit. Authentication
+        uses expiry checks immediately; DynamoDB TTL removal is asynchronous and may happen later. Persistent
+        account and credential associations remain until verified deletion or the applicable verified unlink.
+      </p>
+      <p>
+        Verified account deletion first marks the account for deletion, blocks new actions, linking, resets,
+        sessions and old request replays, and revokes active web sessions and provider tokens. If provider
+        revocation is temporarily unavailable, deletion pauses without losing its reviewed recovery progress.
+        A bounded, resumable job removes persistent mappings and authentication records and deletes gameplay
+        state using the manifest; the manifest is removed last. Temporary records are deleted or made unusable
+        until their stated expiry. Only the minimum non-identifying, replay-prevention deletion tombstone and
+        bounded recovery protection remain for 35 days. Completion time depends on successful verification,
+        retries and provider availability; it is not promised to be immediate. Expiry does not remove the
+        separate 35-day backup recovery window described above. Gameplay reset leaves identity mappings and
+        the deletion manifest in place. Unlinking, gameplay reset and verified account deletion are different
+        operations.
+      </p>
+
@@ -131,4 +212,5 @@
-        Never email passwords, JWTs, Twitch tokens, signing keys, or opaque player IDs. An automated
-        privacy-deletion endpoint does not currently exist. We are completing the deletion mechanism before
-        public release. Using the gameplay reset or stopping use of the extension does not itself complete a
-        verified privacy-deletion request.
+        Never email passwords, JWTs, Twitch tokens, signing keys, invitation URLs, or opaque player IDs.
+        An ownership-checked, resumable account-deletion workflow is available when the relevant authenticated
+        interface is enabled. Otherwise, contact us to arrange verified deletion; an email alone is not proof
+        of account ownership. Stopping use, logging out, unlinking, or resetting gameplay does not itself
+        complete a verified privacy-deletion request.
